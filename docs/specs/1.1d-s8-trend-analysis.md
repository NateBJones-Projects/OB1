# Feature 1.1d-s8: Task engine — `trend_analysis` task type

**Category:** Add handler to task runner

## Done when

- `trend_analysis` task type handler is implemented
- Uses the same computation logic as the `weekly_trends` MCP tool (Feature 4.2)
- Runs on schedule and delivers results via configured channel
- This test works:
  ```
  Run weekly-trends now
  → Returns same output as weekly_trends MCP tool, delivered via email
  ```

## What to build

### Handler logic

Reuse the computation logic from the `weekly_trends` MCP tool (Feature 4.2):
1. Compute topic velocity, type distribution, people frequency, sentiment, capture cadence
2. Format output
3. Deliver via `delivery_channel`

Extract the shared logic into a reusable function that both the MCP tool and this handler call.

### Task registration (seed data)

```sql
insert into scheduled_tasks (name, trigger_type, cron_expression, task_type, gather_config, delivery_channel)
values ('weekly-trends', 'cron', '0 9 * * 0', 'trend_analysis',
  '{"weeks_back": 4}', 'email');
```

## Dependencies

- 1.1d-s2 (cron triggers)
- 4.2 (weekly trends MCP tool) — shares the computation logic
