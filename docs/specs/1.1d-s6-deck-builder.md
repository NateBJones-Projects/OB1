# Feature 1.1d-s6: Task engine — `deck_builder` task type

**Category:** Add handler to task runner

## Status: 🚫 BLOCKED

Cannot build until the Andrea deck template is defined:
- How many slides?
- What content goes on each slide?
- What's the visual layout / branding?
- Is it a standard PPTX template file, or a structure recreated each time?

## Done when

- `deck_builder` task type handler is implemented
- Handler gathers data, populates a slide deck from a template config
- Output is a PPTX file delivered via configured channel
- Andrea deck prep task runs before calendar events

## Dependencies

- 1.1d-s2 (cron triggers)
- 1.1d-s7 (event trigger) — for calendar-based triggering
- Andrea deck template definition (BLOCKING)
